#include "main.h"

/**
 * _abs - compares the absolute value of an integer
 *
 * @n: the value
 * Return: Always 0 on (Success)
 */
int _abs(int n)
{


	if (n >= 0)
		return (n);
	else
		return (-n);
}
