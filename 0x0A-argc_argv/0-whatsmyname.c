#include "main.h"
#include <stdio.h>

/**
 * main - prints program name
 * @argc: The argument count
 * @argv: The argument vector
 *
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);

	return (0);
}
